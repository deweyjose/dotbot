- defaults: 
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.asdf:
    ~/.vim:
    ~/.oh-my-zsh:
    ~/.zshrc:
    ~/bin: bin

- create:
  - ~/repos

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [git update-submodules, Update submodules]

